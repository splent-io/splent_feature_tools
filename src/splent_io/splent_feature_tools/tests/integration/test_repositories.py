"""
Integration tests for splent_feature_tools.

Integration tests exercise services and repositories against a real
test database.  They verify that queries, transactions and model
relationships work correctly — without going through HTTP.
"""


def test_placeholder(test_client):
    """Remove this once you add real integration tests."""
    assert True
